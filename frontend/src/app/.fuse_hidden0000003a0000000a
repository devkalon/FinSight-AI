import type { Metadata } from 'next';
import { IBM_Plex_Sans } from 'next/font/google';
import './globals.css';
import { AuthProvider } from '@/context/AuthContext';
import { AppShell } from '@/components/AppShell';

// Typeface — IBM Plex Sans ("Financial Trust" pairing from the UI/UX Pro Max
// design system): banking-grade, trustworthy, and excellent for dense data.
const plex = IBM_Plex_Sans({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  variable: '--font-sans',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'FinSight AI — Wealth Intelligence & Personal Finance Platform',
  description: 'AI-Powered Personal Finance & Wealth Management Platform with OCR, RAG, Multi-Guru Advisor and Predictive Analytics.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`dark ${plex.variable}`}>
      <body className="bg-[#0F172A] text-[#F8FAFC] min-h-screen">
        <AuthProvider>
          <AppShell>{children}</AppShell>
        </AuthProvider>
      </body>
    </html>
  );
}
